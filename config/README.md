# Workflow configuration
Sylvain Schmitt -
Oct 16, 2024

This folder will contains the files for the workflow configuration. In
particular:

- `config.yml` : the workflow configuration file
- `sites.tsv` : the list of sites to retrieve data from

``` r
fs::dir_tree()
```

    .
    ├── README.md
    ├── README.qmd
    ├── README.rmarkdown
    ├── config.yml
    └── sites.tsv
